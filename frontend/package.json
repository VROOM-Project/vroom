{
  "name": "vroom-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal map UI to build a VROOM request by clicking points and run the solver.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
